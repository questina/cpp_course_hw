on: push

jobs:
  check:
    runs-on: ubuntu-latest
    container:
      image: questina/c_infrastructure:firsttry
      options: --user 1001
    steps:
      - uses: actions/checkout@v2
      - run: make build

  tests:
    runs-on: ubuntu-latest
    container:
      image: questina/c_infrastructure:firsttry
      options: --user 1001
    needs: [run]
    steps:
      - uses: actions/checkout@v2
      - run: make tests